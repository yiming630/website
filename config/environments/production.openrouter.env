# Production Environment Configuration with OpenRouter
# 生产环境配置文件 - 百度服务器部署

# ====================
# OpenRouter API配置（生产环境）
# ====================
OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
OPENROUTER_MODEL=google/gemini-pro
OPENROUTER_FALLBACK_MODEL=google/gemini-flash-1.5
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# ====================
# 本地存储配置（百度服务器）
# ====================
USE_LOCAL_STORAGE=true
LOCAL_STORAGE_ROOT=/data/seekhub/storage
PUBLIC_URL_BASE=https://seekhub.yourdomain.com/files

# ====================
# 数据库配置（生产环境）
# ====================
POSTGRES_USER=${DB_USER}
POSTGRES_PASSWORD=${DB_PASSWORD}
POSTGRES_DB=seekhub_production
POSTGRES_HOST=${DB_HOST}
POSTGRES_PORT=5432

# ====================
# Redis缓存配置（生产环境）
# ====================
REDIS_HOST=${REDIS_HOST}
REDIS_PORT=6379
REDIS_PASSWORD=${REDIS_PASSWORD}

# ====================
# 服务端口配置
# ====================
TRANSLATOR_PORT=8000
API_GATEWAY_PORT=4000
USER_SVC_PORT=4001

# ====================
# 应用URL配置（生产环境）
# ====================
APP_URL=https://seekhub.yourdomain.com
API_URL=https://api.seekhub.yourdomain.com
CLIENT_ORIGIN=https://seekhub.yourdomain.com

# ====================
# JWT认证配置（生产环境）
# ====================
JWT_SECRET=${JWT_SECRET}
JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# ====================
# 翻译服务配置（优化后）
# ====================
MAX_RETRIES=5
RATE_LIMIT_DELAY=0.5
BATCH_SIZE=10
MAX_CONCURRENT_REQUESTS=50
CONNECTION_POOL_SIZE=200

# ====================
# Worker配置（生产环境）
# ====================
MAX_WORKERS=50
WORKER_TIMEOUT=600

# ====================
# 日志配置（生产环境）
# ====================
LOG_LEVEL=warn
LOG_FILE=/var/log/seekhub/production.log

# ====================
# 速率限制（生产环境）
# ====================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# ====================
# Cookie配置（生产环境）
# ====================
COOKIE_NAME=seekhub-token
COOKIE_SECURE=true
COOKIE_HTTP_ONLY=true
COOKIE_SAME_SITE=strict

# ====================
# 监控配置
# ====================
ENABLE_MONITORING=true
METRICS_PORT=9090
HEALTH_CHECK_INTERVAL=30000
